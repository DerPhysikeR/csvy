from setuptools import setup

setup(name='csvy',
      version='0.1',
      description='Read and write csv files with yaml headers',
      author='Paul Reiter',
      author_email='reiter.paul@gmail.com',
      packages=['csvy'],
      zip_safe=False)
